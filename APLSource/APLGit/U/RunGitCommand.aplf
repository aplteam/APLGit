 RunGitCommand←{
⍝ ⍺ ←→ Git project folder
⍝ ⍵ ←→ command
     ⍺←''
     folder←⍺
     c←'git -C "',folder,'" ',⍵,' 2>&1'    ⍝ 2>&1 redirects errors to screen, otherwise we don't see them
     f←{'UTF-8'⎕UCS ⎕UCS ⍵}
     f¨⎕CMD c
 }
